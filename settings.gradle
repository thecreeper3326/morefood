pluginManagement {
	repositories {
		mavenLocal() // This tells Gradle to check your local machine first
		maven {
			name = 'Fabric'
			url = 'https://maven.fabricmc.net/'
		}
		gradlePluginPortal()
	}
}